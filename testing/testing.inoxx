
#include "MyToolz.h"


MyToolz robard();

/* an example of struct */
struct sensors{
  char name[16];
  float value;
};

void setup() {
  Serial.begin(9600);
  while(!Serial);
}

void loop() {

struct sensors structs[] = {
  	{"top", 1.0},
  	{"left", 2.0},
  {"right", 3.0},
  	{"back", 9.9},
  	{"bottom", 0.2 }
};

Serial.println(robard.getSortedNameByPosition(sensors, 0));

}
